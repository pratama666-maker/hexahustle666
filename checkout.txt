<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Pembayaran | Hexahustle</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white min-h-screen">

  <!-- Judul -->
  <div class="p-4 text-center border-b border-gray-700">
    <h1 class="text-2xl font-bold">💳 Pembayaran</h1>
  </div>

  <!-- Form Pembayaran -->
  <form class="p-6 space-y-6" onsubmit="return simpanPembayaran()">
    <!-- Pilih Metode Pembayaran -->
    <div>
      <label for="metode" class="block mb-2 font-medium">Pilih Metode Pembayaran</label>
      <select id="metode" required class="w-full p-2 bg-gray-800 text-white rounded-lg">
        <option value="">-- Pilih Bank/Metode --</option>
        <option value="BCA">BCA</option>
        <option value="BRI">BRI</option>
        <option value="Mandiri">Mandiri</option>
        <option value="Dana">Dana</option>
        <option value="OVO">OVO</option>
        <option value="Gopay">Gopay</option>
        <option value="Bank Jago">Bank Jago</option>
        <option value="CIMB">CIMB</option>
      </select>
    </div>

    <!-- Upload Bukti Transfer -->
    <div>
      <label for="bukti" class="block mb-2 font-medium">Upload Bukti Transfer</label>
      <input type="file" id="bukti" accept="image/*" required class="w-full p-2 bg-gray-800 text-white rounded-lg">
    </div>

    <!-- Tampilkan Preview -->
    <div id="preview" class="hidden">
      <p class="mb-2 text-sm text-gray-400">Preview Bukti Transfer:</p>
      <img id="previewImage" src="" alt="Preview" class="w-full max-w-sm rounded-lg border border-gray-600"/>
    </div>

    <!-- Tombol Kirim -->
    <button type="submit" class="w-full py-2 bg-green-500 hover:bg-green-600 text-white font-semibold rounded-lg transition">
      🚀 Kirim & Simpan Pembayaran
    </button>
  </form>

  <!-- Script -->
  <script>
    const buktiInput = document.getElementById("bukti");
    const preview = document.getElementById("preview");
    const previewImage = document.getElementById("previewImage");

    buktiInput.addEventListener("change", function () {
      const file = this.files[0];
      if (file) {
        const reader = new FileReader();
        reader.onload = function (e) {
          previewImage.src = e.target.result;
          preview.classList.remove("hidden");
        };
        reader.readAsDataURL(file);
      }
    });

    function simpanPembayaran() {
      const metode = document.getElementById("metode").value;
      const bukti = document.getElementById("bukti").files[0];

      if (!metode || !bukti) {
        alert("Lengkapi semua data pembayaran.");
        return false;
      }

      const reader = new FileReader();
      reader.onload = function (e) {
        const pembayaran = {
          metode: metode,
          buktiBase64: e.target.result,
          waktu: new Date().toISOString()
        };
        localStorage.setItem("pembayaranHexahustle", JSON.stringify(pembayaran));
        alert("Pembayaran disimpan di perangkat!");
        window.location.href = "beranda.html"; // Ganti dengan halaman beranda atau sukses
      };
      reader.readAsDataURL(bukti);

      return false; // prevent form submit
    }
  </script>

</body>
</html>
